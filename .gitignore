# 1. 编译输出目录（非常关键，包含二进制 `.elf` / `.hex` / `.o` 文件）
build/
cmake-build-*/

# 2. Clangd 语言服务器的索引缓存文件夹
.clangd/

# 3. VS Code 个人本地配置（建议忽略，防止其他人打开时路径报错）
.vscode/

# 4. CubeMX 产生的临时/备份文件
mx.scratch
*.bak

# 5. 系统文件
.DS_Store
Thumbs.db